/** @type { import('prettier').Config } */
const prettierConfig = {
    semi: false,
    singleQuote: true,
    trailingComma: 'all',
    arrowParens: 'avoid',
    printWidth: 120,
    tabWidth: 4,
    useTabs: false,
    bracketSpacing: true,
}

module.exports = prettierConfig